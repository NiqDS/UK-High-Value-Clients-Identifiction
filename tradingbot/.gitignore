# Secrets — never commit
.env
*.env
!.env.example

# Databases / state
*.db
*.sqlite
*.sqlite3

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Logs
*.log
logs/

# Runtime data / reports / overrides
data/
reports/
runtime_overrides.json

# learning loop drop folder (own losses + uploaded external logs) — keep the README, ignore logs
bad_trades/*
!bad_trades/README.md
