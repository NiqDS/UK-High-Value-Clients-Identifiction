"""tradingbot — risk-first, human-approved automated crypto trading bot.

Build status: Step 1 (scaffold + config + read-only exchange adapter).
The risk engine, execution, events/kill-switch, Telegram approval, and
backtesting land in subsequent milestones — see README "Build Order".
"""

__version__ = "0.1.0"
